# AndroidBaseline
A basic empty Android application that actually compiles

If you start Android Studio, and just pick an empty project, most likely it is not going to compile. The myriad dependencies are often interconnected, and the challenge is to find a combination of library versions that actually work.

This repo is for maintaining a working empty Compose project (with Retrofit and Hilt), that can be built upon.
